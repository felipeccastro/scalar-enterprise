# peewee and bottle are still vendored as plain .py files in vendor/ — see
# vendor/LICENSE.peewee and vendor/LICENSE.bottle (both MIT) — but this tier
# runs against Postgres (see models.py), which needs a real driver:
#   pip install -r requirements.txt
#
# `make` (see Makefile) runs the app under gunicorn instead of `python3
# app.py`, for autoreload + a periodic worker restart — install that too:
#   pip install gunicorn
psycopg2-binary
